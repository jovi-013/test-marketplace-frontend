import React from 'react';
import { Typography, Container } from '@mui/material';

const RegisterPage = () => {
    return (
        <Container>
            <Typography variant="h4" sx={{ mt: 4 }}>Register Page</Typography>
        </Container>
    );
}

export default RegisterPage;